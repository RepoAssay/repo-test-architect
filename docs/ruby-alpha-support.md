# Ruby Alpha Support

The Ruby adapter is supported at the bounded Bundler/Minitest/RSpec boundary below. It flows through project detection, audit, ranking, planning, explanations, findings, placement, stats, CLI/MCP calls, golden artifacts, model consistency, implementation coverage, generated performance checks, and the shared three-role validation corpus without a Ruby-specific artifact format.

## Supported Baseline

| Area | Supported boundary |
| --- | --- |
| Project ownership | One root `Gemfile`; nested directories with their own `Gemfile` are separate detected projects; multiple root gemspecs require a complete exact named set |
| Source ownership | Repository-contained `.rb` files below `lib/`; symlinks and conventional build/output/vendor directories are excluded |
| Package metadata | Bundler through `Gemfile` and optional `Gemfile.lock`; zero or one root `.gemspec`, or multiple root gemspecs whose unique exact static names are each selected once by top-level literal `gemspec name:` declarations; the selected runner may be declared directly in any owned exact file |
| Minitest discovery | Runnable `test/**/*_test.rb` files with a visible `Minitest::Test` subclass and `test_*` method |
| RSpec discovery | Runnable `spec/**/*_spec.rb` files with a visible `RSpec.describe`/`describe` group and `it`/`specify` example |
| Test command | `bundle exec rspec`, `bundle exec rake test` for a conventional default `Rake::TestTask` or `Minitest::TestTask`, or a bounded direct Minitest loader when no conventional Rake task exists |
| Existing-test evidence | `ruby-constant-reference` for a unique visible space-indented class/module constant reached through at most three exact literal repository-owned load edges; a uniquely resolved literal root `.rspec --require` helper or exact conventional per-file RSpec `require "spec_helper"` may consume the first edge, bare requires use `lib/` only for one root gemspec or a complete exact named root-gemspec set included by Gemfile, relative requires resolve from their file, one direct source edge is `direct`, helper/entrypoint reachability is `referenced`, exact source-owned singleton/constructor calls and direct instance calls through one immutable constructor-local receiver in runnable bodies are `called`, RSpec `described_class` may replace the receiver only under its nearest exact constant-owned `describe`, one-line `let`/`subject` definitions may own a direct exact constructor receiver, and one exact source-factory or same-group RSpec helper constructor return may feed the same stable-local receiver rule; selected Minitest/RSpec assertions are `asserted`, and a unique basename remains `naming` fallback |
| Shared-example binding | Exact same-file, earlier, literal `shared_examples`/`shared_examples_for` declarations selected uniquely by a direct constant-owned literal `it_behaves_like`/`include_examples`; the inclusion owner binds `described_class`, while dormant bodies contribute no usage |
| Candidate filtering | Exact repository-relative, absolute, and Windows-separated `changedPaths` |
| Native fixture gate | `bundle check` followed by `bundle exec rake test` |
| Generated scale gate | 400 behavioral source files, 200 runnable Minitest files, exactly 200 covered and 200 untested candidates, and 200 naming relationships under 5 seconds |

The adapter masks ordinary Ruby comments and quoted strings before recognizing runnable tests, Rake tasks, methods, constants, references, and behavior signals. For multiple root gemspecs, each file must declare one unique exact name through `spec.name = "NAME"` or the first literal argument to `Gem::Specification.new`, and the root Gemfile must select the complete set exactly once with top-level `gemspec name: "NAME"` or `gemspec(name: "NAME")`. For RSpec, a safe literal `--require NAME` or `--require=NAME` in the root `.rspec` may resolve uniquely to one root or `spec/` helper and consumes one edge in the three-edge budget. A runnable `spec/**/*_spec.rb` file may also load exactly `spec/spec_helper.rb` through a zero-indented literal `require "spec_helper"` or `require("spec_helper")`; this conventional runner path wins over a same-named file below `lib/` and consumes the first edge only for that declaring test. Exact literal `require_relative` edges resolve from the owning file; every other bare `require` maps into `lib/` only when one root gemspec or the complete named set is statically included by the root Gemfile. Every edge must resolve to a repository-owned `.rb` file. A constant must have one owner among the source files reachable by that test; multiple reachable reopenings are withheld.

Usage is narrower than constant evidence. Only a conventional `def test_*` body or RSpec `it`/`specify` body can contribute it. An exact `Constant.method` call must match a directly declared `def self.method`, a method directly inside `class << self`, or `.new` backed by that class's direct `initialize`; all declarations use two-space lexical indentation. In RSpec, `described_class` may stand in for that exact constant only when the nearest containing static constant-first `describe` owns it; nested string groups and contexts inherit the owner, while a nearer exact constant group overrides it. One local assigned exactly once from that direct, unchained constructor may then call a directly declared instance method when the class does not override `.new` and the local is not block-shadowed. The same instance rule applies to `let(:name) { Constant.new(...) }`, `let(:name) { described_class.new(...) }`, named `subject`, and unnamed `subject` when the definition is one line, unchained, has no constructor block, belongs to a normal containing group, and remains the nearest declaration for that receiver. It also applies when the stable local receives one exact source singleton factory whose entire body is `new(...)` or `self.new(...)`, or one exact same-file RSpec helper whose entire body is `Constant.new(...)` or group-owned `described_class.new(...)`. Helper ownership follows normal group inheritance and nearest-declaration override rules; unknown inner helpers and cross-owner `described_class` block identity. A same-file `shared_examples` or `shared_examples_for` body may contribute only after one uniquely visible earlier declaration is selected by an exact literal `it_behaves_like` or `include_examples` call directly inside a constant-owned normal group. Direct and exactly parenthesized literal forms are accepted; the inclusion owner's `described_class` is applied to the shared example bodies. Dormant shared bodies contribute no usage. Such a call is `called`. It becomes `asserted` when the same line is a selected built-in Minitest assertion or RSpec `expect`, or when one unique unreassigned local receives the exact call and is later consumed by one of those assertions. A matching filename remains intentionally weaker `naming` evidence.

## Explicit Blockers And Exclusions

The first slice does not claim support for:

- Rails application ownership or Rails-native test commands
- Minitest spec-style `describe`/`it` discovery and custom setup-partitioned test commands
- mixed Minitest and RSpec command ownership in one project
- partial, duplicate, unknown, nested, computed, path-based, hash-rocket, extra-option, or dynamically named multi-gemspec ownership
- custom test roots, custom Rake task names, or computed runner commands
- test-unit, Cucumber, Capybara, property testing, mutation testing, or other Ruby test frameworks
- Bundler workspaces, path-gem graphs, engines, or umbrella repositories beyond separately detected literal `Gemfile` roots
- multiline, eager, chained, block-owned, aliased, or otherwise computed RSpec `let`/`subject`; memo or helper definitions inside shared examples; implicit `should`/`is_expected`/`its` usage; Minitest, cross-file/module, setup-hook, chained, wrapped, multi-statement, lexically unqualified, or otherwise computed helper/factory returns; helper parameters as receiver types; fields; constructor overrides; dynamic/string/variable/parenthesized `describe` ownership; `described_class` aliases or use outside runnable examples; attributes/delegators, `extend self`, inherited/mixed-in/generated methods, custom assertion methods, multiline assertion flow, or deeper result flow
- parameterized, cross-file, dynamically named, duplicate-nearest, block-bearing, nested-chain, shared-context, `it_should_behave_like`, or otherwise computed RSpec shared-example declarations and inclusions
- computed/dynamic requires, alternate, nested, computed, or explicit-path per-file RSpec helpers, dynamic or nested `.rspec` configuration, ambiguous configured helpers, graphs deeper than three edges, tab-indented or dynamic constant declarations, constant assignment/`Class.new`, ambiguous reachable reopenings, partial or unqualified nested constant references
- metaprogrammed tests or behavior, dynamically defined methods, deferred lambdas, `public_send`/`send`, refinements, `autoload`, Zeitwerk inference, or runtime constant lookup
- heredoc, percent-literal, interpolation, or parser-complete Ruby semantics

These shapes remain blocked or receive conservative missing evidence rather than inferred coverage.

## Promotion Result

Ruby fills the shared promotion corpus with three exact public-repository pins:

- rubyzip is the conventional-library/service role at `4209b022069d4d5646753dd5799e8771e4699e5c`
- Faraday is the framework-heavy role at `3725183bead9939d3575f2df8e16d8ca7acddf5e`
- Diplomat is the difficult-ownership role at `313f94ff2582c7eb161e2aa775182db10ad14285`

All 21 Ruby detection, ownership, command, evidence, ranking, stability, and performance areas pass. Clean five-run measurements are canonical-digest stable: rubyzip reports 19 untested, 23 covered, 6 skipped, 80 relationships, and a 46 ms median; Faraday reports 8 untested, 22 covered, 3 skipped, 45 relationships, and a 59 ms median; Diplomat reports 8 untested, 21 covered, 3 skipped, 26 relationships, and a 56 ms median. Their selected native commands pass 412 Minitest runs with 2,820 assertions, 639 Faraday RSpec examples, and 281 Diplomat RSpec examples with no failures.

The native fixture, shared conformance, implementation coverage, generated performance, golden/model-consistency, packaging, and cross-platform gates provide the same promotion infrastructure as the other supported adapters. Supported maturity remains limited to this matrix; Rails, arbitrary DSL evaluation, and the exclusions above are not implied.

## Checked-In Proof

`examples/ruby-minitest-basic` contains one locked Bundler gem, a conventional Rake Minitest task, one parser test with an exact source require and visible unique constant, one untested service candidate, and module/data wiring that is deferred to consuming behavior. Run:

```text
npm run ruby:native:check
npm run ruby:performance:check
```

Exact group-owned `described_class`, direct one-line constructor memos, complete named root-gemspec ownership, conventional per-file RSpec `spec_helper` loading, exact source-factory or same-group RSpec helper constructor returns, and exact same-file literal shared-example binding are now bounded. Multiline/eager memos, implicit matcher semantics, deeper result flow, and Rails ownership remain later comparison points.

The first live probe, [`rubyzip/rubyzip`](https://github.com/rubyzip/rubyzip) at `4209b022069d4d5646753dd5799e8771e4699e5c`, passed 412 native Minitest runs and 2,820 assertions with no failures. Its foundation pass added exact `Minitest::TestTask.create` command ownership and root-gemspec runner declarations. The first evidence follow-up moved the audit from 23/19 to 19/23 untested/covered through bounded require/constant attribution. The receiver follow-up preserves that graph while separating 15 asserted, 24 called, 38 reference-only, and 3 naming relationships: it recovers one direct `Zip::File#find_entry` assertion and removes two optimistic generated-reader assertions. See the [rubyzip Live Validation Report](ruby-rubyzip-validation-report.md).

The second live probe, [`lostisland/faraday`](https://github.com/lostisland/faraday) at `3725183bead9939d3575f2df8e16d8ca7acddf5e`, passed 639 native RSpec examples with no failures and 95.04% reported line coverage. Exact root `.rspec` helper ownership changes its unchanged 8/22 candidate split from 22 naming-only links to 45 relationships: 32 exact and 13 naming. Direct one-line memo pressure moves three reviewed Connection, Error, and Response relationships to asserted usage while preserving the graph, leaving 6 asserted, 1 called, 25 exact reference-only, and 13 naming relationships. See the [Faraday Live Validation Report](ruby-faraday-validation-report.md).

The third live probe, [`thoughtbot/factory_bot`](https://github.com/thoughtbot/factory_bot) at `967d128e3710b9918f97c75b47b11251e85c2377`, passed 764 adapter-selected RSpec examples and the broader upstream 764-example plus 4-scenario suite. Exact group-owned `described_class` preserves its 21/28 candidate split and 56 relationships while moving four reviewed exact links to asserted usage, producing 21 asserted, 2 called, 20 exact reference-only, and 13 naming relationships. The memo follow-up leaves that graph unchanged because the remaining `subject` shapes use inherited/generated methods, constructor blocks, shared examples, or implicit matchers. See the [Factory Bot Live Validation Report](ruby-factory-bot-validation-report.md).

The fourth live probe, [`WeAreFarmGeek/diplomat`](https://github.com/WeAreFarmGeek/diplomat) at `313f94ff2582c7eb161e2aa775182db10ad14285`, passed 281 native RSpec examples. Its two exact named root gemspecs form one high-confidence Ruby gem with `bundle exec rspec`; its 23 exact per-file `require "spec_helper"` declarations now move `lib/diplomat.rb` from untested to covered, producing an 8/21/3 candidate split with 6 exact and 20 naming links. The helper's dynamic internal fan-out remains uncredited. See the [Diplomat Live Validation Report](ruby-diplomat-validation-report.md).

The fifth live probe, [`dinie-tech/sdk-ruby`](https://github.com/dinie-tech/sdk-ruby) at `11048303d772928775ca35e05465749dada24f78`, passed 579 native RSpec examples. Exact same-group helpers returning `described_class.new(...)` preserve its 26/19/11 candidate split and 47 relationships while moving three reviewed `HttpClient` and `TokenManager` links to asserted usage. The complementary CGRateS pin passed 27 examples and remains audit-identical because its helpers require unqualified lexical constant resolution. See the [Helper And Factory Return Live Validation Report](ruby-helper-return-validation-report.md).

The sixth live pressure pass revisits Faraday and Factory Bot for shared-example semantics. Faraday passed 639 examples at 95.04% line coverage and preserves its 8/22/3 candidate split, 45 relationships, and 6 asserted / 1 called / 25 exact reference-only / 13 naming distribution while exercising exact literal same-file inclusions. Factory Bot passed 764 examples at 98.51% line coverage and remains audit-identical because its prominent shared examples require parameters, inclusion blocks, legacy aliases, memos, or implicit matchers outside this slice. See the [Shared Example Live Validation Report](ruby-shared-example-validation-report.md).

The first blind post-promotion pass selected [`licensee/licensed`](https://github.com/licensee/licensed) at `2db4c2a2743e159e9bf1931c9c3b2df2de8ff4bf` before inspecting its output. Its 51 test files contain 235 Minitest `describe` groups and 631 `it` examples but no supported `test_*` methods, so the adapter correctly stays at medium confidence with no command or evidence instead of confusing the DSL with RSpec. Five audits are stable at 48/0/5 with zero relationships and a 20 ms median. The conventional default Rake task fails without the repository's per-ecosystem setup matrix, while the custom upstream `script/test core` command passes 330 runs and 1,063 assertions. Minitest spec discovery and custom suite-command ownership therefore remain one joint future decision. See the [Licensed Post-Promotion Validation Report](ruby-licensed-post-promotion-validation-report.md).
