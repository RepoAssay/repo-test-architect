{
  "schemaVersion": "model-consistency-scenario/v1",
  "id": "go-testing-basic-plan",
  "description": "Different model profiles should preserve bounded Go planning decisions, including direct top-level and explicitly typed receiver-method evidence, one-hop same-package dependency evidence, and DTO deferral.",
  "sourceArtifact": {
    "path": "evals/expected/go-testing-basic.audit.json",
    "schemaVersion": "audit/v1"
  },
  "toolCall": {
    "toolName": "generate_test_plan",
    "arguments": {}
  },
  "lockedFields": [
    {
      "path": "summary.verificationCommand",
      "expected": "go test ./...",
      "reason": "A conventional single-module standard-library test suite should retain the deterministic Go verification command."
    },
    {
      "path": "summary.addTestCount",
      "expected": 0,
      "reason": "Every runtime target in the fixture now has bounded test evidence."
    },
    {
      "path": "summary.extendTestCount",
      "expected": 3,
      "reason": "The directly exercised parser, explicitly typed payment-client method, and one-hop normalizer dependency should remain extend-test items."
    },
    {
      "path": "summary.deferredCount",
      "expected": 1,
      "reason": "The struct-only payment shape should remain deferred from direct testing."
    },
    {
      "path": "items[0].id",
      "expected": "extend-test:price_parser.go",
      "reason": "Covered transformation logic should keep a stable extend-test item id."
    },
    {
      "path": "items[0].existingTestEvidence[0].kind",
      "expected": "go-symbol-reference",
      "reason": "The parser test must retain its exact package-symbol proof."
    },
    {
      "path": "items[0].existingTestEvidence[0].usage",
      "expected": "called",
      "reason": "The imported parser function remains directly called by the runnable Go test."
    },
    {
      "path": "items[1].id",
      "expected": "extend-test:payment_client.go",
      "reason": "The external boundary should retain a stable extension item after its explicitly typed receiver call is proven."
    },
    {
      "path": "items[1].existingTestEvidence[0].kind",
      "expected": "go-symbol-reference",
      "reason": "The payment client must retain exact receiver-method provenance."
    },
    {
      "path": "items[1].existingTestEvidence[0].usage",
      "expected": "called",
      "reason": "The explicitly typed payment client remains directly called by the runnable test."
    },
    {
      "path": "items[2].id",
      "expected": "extend-test:price_normalizer.go",
      "reason": "The bounded same-package dependency should remain a covered normalizer item."
    },
    {
      "path": "items[2].existingTestEvidence[0].kind",
      "expected": "go-source-dependency",
      "reason": "The normalizer should retain explicit one-hop Go source dependency provenance."
    },
    {
      "path": "items[2].existingTestEvidence[0].viaUsage",
      "expected": "called",
      "reason": "The dependency evidence must remain rooted in a directly called parser entrypoint."
    },
    {
      "path": "items[3].id",
      "expected": "defer:payment.go",
      "reason": "The struct-only DTO should remain covered through consuming behavior."
    }
  ],
  "allowedVariations": [
    "summary wording",
    "Go explanation wording",
    "optional detail depth"
  ],
  "unexpectedVariations": [
    "missing go test verification command",
    "missing direct Go symbol evidence",
    "missing bounded same-package dependency evidence",
    "dependency evidence propagated beyond one source hop",
    "direct DTO test recommendation",
    "receiver-method evidence downgraded to a type reference",
    "different plan action counts"
  ]
}
