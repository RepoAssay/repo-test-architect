{
  "schemaVersion": "model-consistency-scenario/v1",
  "id": "python-package-local-tests-plan",
  "description": "Different model profiles should preserve Python package-local test planning decisions, including existing test evidence and direct DTO deferral.",
  "sourceArtifact": {
    "path": "evals/expected/python-package-local-tests.audit.json",
    "schemaVersion": "audit/v1"
  },
  "toolCall": {
    "toolName": "generate_test_plan",
    "arguments": {}
  },
  "lockedFields": [
    {
      "path": "summary.verificationCommand",
      "expected": "pytest",
      "reason": "Package-local pytest fixtures must preserve the repo-native verification command."
    },
    {
      "path": "summary.addTestCount",
      "expected": 1,
      "reason": "The untested discount service should remain an add-test item."
    },
    {
      "path": "summary.extendTestCount",
      "expected": 1,
      "reason": "The covered calculator should remain an extend-test item."
    },
    {
      "path": "summary.deferredCount",
      "expected": 1,
      "reason": "The DTO response should stay deferred from direct unit testing."
    },
    {
      "path": "items[0].id",
      "expected": "extend-test:src/checkout/calculator.py",
      "reason": "Package-local covered calculator logic should keep a stable extend-test item id."
    },
    {
      "path": "items[0].existingTestPaths[0]",
      "expected": "src/checkout/tests/test_calculator.py",
      "reason": "The adapter must preserve package-local pytest evidence."
    },
    {
      "path": "items[0].existingTestEvidence[0].kind",
      "expected": "python-module-import",
      "reason": "Package-local coverage must retain its package-qualified Python import proof."
    },
    {
      "path": "items[0].existingTestEvidence[0].usage",
      "expected": "asserted",
      "reason": "The calculator result remains directly asserted by the package-local test."
    },
    {
      "path": "items[1].id",
      "expected": "add-test:src/checkout/discount_service.py",
      "reason": "Untested service code should stay a direct unit-test candidate."
    },
    {
      "path": "items[2].id",
      "expected": "defer:src/checkout/checkout_response.py",
      "reason": "DTO-only dataclass responses should stay covered through consuming boundary tests."
    }
  ],
  "allowedVariations": [
    "summary wording",
    "Python explanation wording",
    "optional detail depth"
  ],
  "unexpectedVariations": [
    "missing pytest verification command",
    "missing package-local existing test evidence",
    "direct DTO test recommendation",
    "missing discount service candidate"
  ]
}
